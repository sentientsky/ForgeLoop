# Architecture Language

Use these terms consistently:

- Boundary: the line between responsibilities.
- Surface: the public behaviour callers rely on.
- Core: logic that owns product behaviour.
- Adapter: code that talks to an external tool or format.
- Policy: a rule that shapes decisions.
- Mechanism: code that executes a policy.

Good architecture makes the boundary obvious and the surface easy to test.

