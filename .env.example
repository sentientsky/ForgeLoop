# ForgeLoop environment example
#
# This file is safe to commit because values are blank.
# Create the real external file with:
#
#   python -m forgeloop secrets init .
#
# Then fill values in the external file printed by:
#
#   python -m forgeloop secrets path .

FORGELOOP_ANTHROPIC_API_KEY=
FORGELOOP_OPENAI_API_KEY=
FORGELOOP_GITHUB_TOKEN=
FORGELOOP_MEMORY_ENCRYPTION_KEY=
